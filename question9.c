#include<stdio.h>
#include<string.h>
char revWord(char str[],int i)
{
    
}

int main()
{
    char str[40];int i=0;
    printf("Enter string:\n");
    fgets(str,40,stdin);
    revWord(str,i);
    printf("%s %d",str,i);
    return 0;
}